
numLions <- 42
numTigers <- 17
country <- "South Africa"

msg1 <- paste('The number of lions in', country, 'is', numLions, sep = " ")
msg2 <- paste('The number of tigers in', country, 'is', numTigers, sep = " ")
msg <- paste(msg1, '. ', msg2, '.', sep = "");

print(msg)

